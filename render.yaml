services:
  - type: web
    name: meck-property-search
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn meck_property_search.web:app
    envVars:
      - key: FLASK_DEBUG
        value: "0"
